namespace CoHAnalytics.Services;

public enum HomecomingDiscoverySource
{
    Persisted,
    OfficialDefault,
    DesktopShortcut,
    StartMenuShortcut,
    RunningClient,
    RunningLauncher,
    Registry,
    CommonPath
}
